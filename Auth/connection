<?php 

$server = "localhost";
$user = "root";
$pass = "";
$database = "api-laptop";

$conn = mysqli_connect($server, $user, $pass, $database);

if (!$conn) {
    die("<script>alert('maaf anda belum terhubung ke database.')</script>");
}
?>
